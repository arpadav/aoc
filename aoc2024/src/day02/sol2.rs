use super::INPUT;

pub fn solve() {
    
}

fn solution_1() -> usize {
    // let mut game = Game::new();
    // INPUT
    //     .lines()
    //     .fold(0, |acc, line| acc + game.calculate_power(line.as_bytes()) )
    0
}